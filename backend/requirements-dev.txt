# Test-only dependencies. Do NOT add these to requirements.txt / the deployed
# function — production runtime only needs curl-cffi, not httpx.
pytest
httpx        # or httpx2, which Starlette's TestClient now prefers
